map returns a new array

var arr = [1,2,3,4,5,10]

arr.reduce(funciton(previous, current, array, index) {
	return previous + current;
})

//apply, call, bind
call changes this basicall.


//filter, map, reduce

//&& || ?:
