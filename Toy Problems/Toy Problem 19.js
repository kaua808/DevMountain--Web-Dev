//make a function that takes a string and oders it alphabetically 
//hello ==> ehllo

var order = function(str) {
    str = str.split("").sort().join("");
    return str;          
}

alert(order("hello"));

//doing it without.sort

var order = function(str) {
	str = str.split("")

	for (var i = 0; i < str.length; i++) {
		for (var j = 0; j < i; j++) {
			if(arr[i] > arr[i]) {
				var temp = arr[i]
				arr[i] = 
			} 
		}
	}
	return str
}

